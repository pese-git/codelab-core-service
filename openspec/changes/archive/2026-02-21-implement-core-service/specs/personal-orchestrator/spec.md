# Спецификация: Personal Orchestrator

## 🎯 Архитектурное решение: Unified UserAgent подход

**CRITICAL DESIGN DECISION**: Все агенты в системе (Architect, Code, Ask, Debug, Orchestrator, Custom) хранятся в единой таблице `user_agents` с разными значениями поля `config['metadata']['role']`.

**НЕ используется**: Отдельная таблица `user_orchestrators` — это не соответствует текущему дизайну и должна быть удалена (см. ORCHESTRATOR_UNIFIED_AGENT_IMPLEMENTATION_PLAN.md).

**Преимущества unified подхода**:
- DRY: одна модель, одна таблица, одна логика CRUD
- Оптимально: нет JOIN'ов, оптимальные query'и
- Масштабируемо: легко добавлять новые типы агентов
- Гибко: несколько оркестраторов с разными стратегиями

---

## CRITICAL: User-Defined Agents Architecture

### Requirement: Агенты как пользовательские сущности (UserAgent)
Orchestrator ДОЛЖЕН работать с пользовательскими агентами из БД (UserAgent model) с разными ролями (`role` в `config['metadata']`), а не с жёстко закодированными типами.

#### Scenario: Загрузка агентов для проекта
- **WHEN** Orchestrator маршрутизирует сообщение в проекте
- **THEN** он загружает всех агентов из БД: `SELECT * FROM user_agents WHERE project_id = ? AND status = 'ready'`
- **AND** все агенты хранятся в ОДНОЙ таблице `user_agents` с разными ролями
- **AND** никогда не предполагает наличие конкретного агента

#### Scenario: Структура UserAgent с ролями
- **WHEN** Orchestrator работает с агентом
- **THEN** все агенты имеют одинаковую структуру в БД:
  ```
  user_agents:
    - id (UUID)
    - user_id (FK → users)
    - project_id (FK → user_projects)
    - name (string, e.g., "Architect", "Orchestrator", "Code")
    - config (JSON):
        - model (string)
        - metadata:
            - role (string): "architect" | "orchestrator" | "code" | "ask" | "debug" | "custom"
            - capabilities (array)
            - cost_per_call (float)
            - estimated_duration (float)
    - status (string): "ready" | "busy" | "error"
  ```
- **AND** различные агенты отличаются ТОЛЬКО значением `config['metadata']['role']`

#### Scenario: Извлечение capabilities из config.metadata
- **WHEN** Orchestrator анализирует агентов для маршрутизации
- **THEN** он извлекает capabilities из поля `user_agents.config['metadata']['capabilities']` (JSON array)
- **EXAMPLE**: `config['metadata'] = {"role": "code", "capabilities": ["implement_feature", "fix_bug", "code_review"], "cost_per_call": 0.03}`

#### Scenario: Дефолтный starter pack (Unified UserAgent подход)
- **WHEN** пользователь создаёт новый проект
- **THEN** автоматически создаются 5 дефолтных UserAgent'ов:
  - Ask Agent: role="ask", capabilities=["answer_question", "explain_concept"]
  - Debug Agent: role="debug", capabilities=["investigate_error", "add_logging"]
  - Code Agent: role="code", capabilities=["implement_feature", "fix_bug", "code_review"]
  - Architect Agent: role="architect", capabilities=["design_architecture", "create_specifications", "plan_tasks"]
  - Orchestrator Agent: role="orchestrator", capabilities=["workflow_management", "task_routing", "dependency_tracking"]
- **AND** все хранятся в единой таблице `user_agents` с разными значениями role

#### Scenario: Пользователь добавляет кастомного агента
- **WHEN** пользователь создаёт агента через API: `POST /my/projects/{project_id}/agents/`
- **THEN** новый UserAgent сохраняется в БД с пользовательскими capabilities и system_prompt
- **AND** при следующем планировании Orchestrator может использовать этого агента

#### Scenario: Пользователь обновляет capabilities агента
- **WHEN** пользователь обновляет агента: `PUT /my/projects/{project_id}/agents/{agent_id}`
- **THEN** изменения сохраняются в `user_agents.config` (JSON field)
- **AND** Orchestrator использует обновлённые capabilities при следующем планировании

#### Scenario: Пользователь удаляет агента
- **WHEN** пользователь выполняет `DELETE /my/projects/{project_id}/agents/{agent_id}`
- **THEN** агент удаляется из БД (cascade delete на related entities)
- **AND** Orchestrator больше не может использовать этого агента

#### Scenario: Изоляция агентов между проектами
- **WHEN** Orchestrator планирует для project_id=A и user_id=X
- **THEN** он загружает только: `SELECT * FROM user_agents WHERE user_id=X AND project_id=A`
- **AND** агенты из project_id=B недоступны

#### Scenario: Изоляция агентов между пользователями
- **WHEN** Orchestrator планирует для user_id=1
- **THEN** он видит только: `SELECT * FROM user_agents WHERE user_id=1`
- **AND** агенты user_id=2 полностью недоступны (даже на уровне БД)

#### Scenario: Агент недоступен при плохом health
- **WHEN** агент имеет status='error' или status='busy'
- **THEN** Orchestrator исключает его из доступных при планировании
- **AND** выбирает агент только если status='ready'

## ADDED Requirements

### Requirement: Маршрутизация сообщений на основе capabilities агентов [PHASE 1 - CRITICAL]

**Ответственность Orchestrator System**: Выбирать лучшего агента для сообщений пользователя на основе анализа capabilities.

**Примечание**: Создание плана с графом задач — это ответственность Architect Agent, а не Orchestrator System.
Orchestrator выполняет маршрутизацию отдельных сообщений и координирует выполнение готовых планов.

#### Scenario: Анализ запроса для выбора агента
- **WHEN** пользователь отправляет сообщение БЕЗ явного указания target_agent
- **THEN** Orchestrator анализирует текст сообщения
- **AND** определяет требуемые capabilities (ключевые слова, контекст)
- **EXAMPLE**: "Отладь баг в auth.py" → требуются capabilities: ["debug", "code_analysis"]

#### Scenario: Выбор агента с максимальным соответствием
- **WHEN** Orchestrator определил требуемые capabilities
- **THEN** он получает всех агентов проекта со статусом='ready'
- **AND** для каждого агента получает capabilities из `user_agents.config['metadata']['capabilities']`
- **AND** вычисляет score соответствия (overlap между требуемыми и доступными capabilities)
- **AND** выбирает агента с наибольшим score

#### Scenario: Возврат confidence score
- **WHEN** агент выбран
- **THEN** Orchestrator возвращает routing_score отражающий реальное соответствие (0.0-1.0)
- **EXAMPLE**: 
  - Запрос "Отладь баг" требует ["debug"], Code Agent имеет ["debug", "fix_bug"] → score = 0.95
  - Запрос "Отладь баг" требует ["debug"], Ask Agent имеет ["explain"] → score = 0.1

#### Scenario: Fallback при отсутствии подходящего агента
- **WHEN** ни один агент не имеет требуемые capabilities
- **THEN** Orchestrator выбирает агента с наибольшим частичным соответствием
- **AND** возвращает низкий confidence score (0.3-0.5) чтобы клиент знал о неуверенности

#### Scenario: Исключение недоступных агентов
- **WHEN** агент имеет status != 'ready'
- **THEN** Orchestrator исключает его из выбора
- **AND** не рассматривает его в маршрутизации

### Requirement: Планирование графа задач из natural language [PHASE 2 - ARCHITECT AGENT]

**Ответственность Architect Agent**: Анализировать запрос и создавать граф задач для выполнения.

**Примечание**: Это требование реализуется Architect Agent (LLM), а не Orchestrator System.

#### Scenario: Простой запрос с одной задачей
- **WHEN** Architect получает запрос "Создай функцию для проверки email"
- **THEN** Architect анализирует и создает план с одной задачей: "implement_function"
- **AND** ассигнует эту задачу агенту с capabilities ["implement_feature"]

#### Scenario: Сложный запрос с несколькими задачами
- **WHEN** Architect получает запрос "Найди информацию о FastAPI и создай REST API"
- **THEN** Architect создает граф с задачами: 
  1. research_framework (assign to Ask Agent)
  2. implement_api (assign to Code Agent)
  - С зависимостью: research_framework → implement_api

#### Scenario: Запрос с параллельными задачами
- **WHEN** Architect получает запрос с независимыми компонентами
- **THEN** он создает граф где независимые задачи помечены как параллельно выполняемые
- **EXAMPLE**: "Напиши unit tests и integration tests" → 2 параллельные задачи для Code Agent

#### Scenario: Невозможность выполнения
- **WHEN** запрос требует capabilities которых нет у доступных агентов
- **THEN** Architect возвращает plan с статусом "not_possible"
- **AND** объясняет какие capabilities требуются

### Requirement: Анализ зависимостей между задачами [PHASE 2 - ARCHITECT AGENT]

**Ответственность Architect Agent**: Определять зависимости и строить корректный граф задач.

#### Scenario: Последовательные зависимости
- **WHEN** Architect анализирует план где задача B требует результата A
- **THEN** он добавляет зависимость A → B в граф

#### Scenario: Множественные зависимости
- **WHEN** задача C требует результатов задач A и B
- **THEN** Architect создает зависимости A → C и B → C

#### Scenario: Обнаружение циклических зависимостей
- **WHEN** Architect обнаруживает цикл (A → B → C → A)
- **THEN** он отмечает план как invalid и объясняет проблему

#### Scenario: Независимые задачи
- **WHEN** задачи A и B не зависят друг от друга
- **THEN** Architect помечает их как параллельно выполняемые (same_level: true)

### Requirement: Топологическая сортировка для параллельного выполнения [PHASE 2 - ORCHESTRATOR SYSTEM]

**Ответственность Orchestrator System**: Сортировать готовый граф задач и выполнять их в правильном порядке.

#### Scenario: Определение уровней выполнения
- **WHEN** граф задач построен и одобрен
- **THEN** Orchestrator System группирует задачи по уровням где задачи одного уровня могут выполняться параллельно

#### Scenario: Максимальный параллелизм
- **WHEN** граф содержит независимые задачи
- **THEN** Orchestrator System планирует их выполнение одновременно с учетом concurrency_limit (max 3 агента)

#### Scenario: Последовательное выполнение при зависимостях
- **WHEN** все задачи зависят друг от друга последовательно
- **THEN** Orchestrator System планирует их выполнение по одной

#### Scenario: Оптимизация порядка
- **WHEN** существует несколько валидных порядков выполнения
- **THEN** Orchestrator System выбирает порядок минимизирующий общее время выполнения

### Requirement: Оценка стоимости выполнения плана [PHASE 2 - ORCHESTRATOR SYSTEM]

**Ответственность Orchestrator System**: Оценивать стоимость готового плана.

#### Scenario: Оценка стоимости LLM вызовов
- **WHEN** план создан (граф задач построен)
- **THEN** Orchestrator System оценивает количество LLM API calls и их стоимость на основе моделей агентов
- **AND** использует cost_per_call из `user_agents.config['metadata']['cost_per_call']`

#### Scenario: Оценка стоимости embeddings
- **WHEN** план включает задачи с RAG поиском
- **THEN** Orchestrator System добавляет стоимость генерации embeddings к общей оценке

#### Scenario: Диапазон стоимости
- **WHEN** точная стоимость неизвестна
- **THEN** Orchestrator System предоставляет диапазон (min-max) стоимости

#### Scenario: Предупреждение о высокой стоимости
- **WHEN** оценочная стоимость превышает $0.10
- **THEN** Orchestrator System отмечает план как нуждающийся в approval

### Requirement: Оценка времени выполнения плана [PHASE 2 - ORCHESTRATOR SYSTEM]

**Ответственность Orchestrator System**: Оценивать время выполнения готового плана.

#### Scenario: Оценка времени для простого плана
- **WHEN** план содержит одну задачу
- **THEN** Orchestrator System оценивает время как estimated_duration агента из config

#### Scenario: Оценка времени для последовательного плана
- **WHEN** план содержит последовательные задачи
- **THEN** Orchestrator System суммирует время выполнения всех задач

#### Scenario: Оценка времени для параллельного плана
- **WHEN** план содержит параллельные задачи
- **THEN** Orchestrator System оценивает время как максимум среди параллельных веток

#### Scenario: Диапазон времени
- **WHEN** время выполнения неопределенно
- **THEN** Orchestrator System предоставляет диапазон (min-max) в секундах

### Requirement: Интеграция с Approval Manager для плана [PHASE 2 - ORCHESTRATOR SYSTEM]

**Ответственность Orchestrator System**: Отправлять план на одобрение если требуется.

**Требование**: Все планы, которые содержат 3+ задачи ИЛИ оценочная стоимость > $0.10, ДОЛЖНЫ быть подтверждены пользователем перед выполнением.

#### Scenario: Автоматическое выполнение простого плана
- **WHEN** план содержит ≤2 задачи И оценочная стоимость ≤ $0.10
- **THEN** Orchestrator System может выполнить план без approval

#### Scenario: Запрос approval для сложного плана
- **WHEN** план содержит 3+ задачи ИЛИ оценочная стоимость > $0.10
- **THEN** Orchestrator System отправляет план в ApprovalManager перед выполнением
- **AND** включает детали: список задач, граф зависимостей, назначенные агенты, оценку стоимости и времени

#### Scenario: Выполнение после approval
- **WHEN** пользователь одобрает план через ApprovalManager
- **THEN** Orchestrator System начинает выполнение согласно графу задач

#### Scenario: Отмена при reject
- **WHEN** пользователь отклоняет план
- **THEN** Orchestrator System отменяет выполнение и возвращает сообщение об отмене

#### Scenario: Timeout approval
- **WHEN** пользователь не отвечает на approval request в течение 300 секунд
- **THEN** Orchestrator System автоматически отклоняет план

#### Scenario: Обработка ошибок при выполнении плана
- **WHEN** одна из задач в одобренном плане завершается с ошибкой
- **THEN** Orchestrator System обрабатывает ошибку согласно зависимостям:
  - Пропускает зависимые задачи если нужны результаты ошибившейся задачи
  - Продолжает независимые задачи
  - Отправляет SSE события о статусе

### Requirement: Выбор агентов для задач плана [PHASE 2 - ARCHITECT AGENT]

**Ответственность Architect Agent**: Назначать агентов на задачи при создании плана.

#### Scenario: Выбор по типу задачи
- **WHEN** задача требует написания кода
- **THEN** orchestrator назначает задачу агенту с capability "coding"

#### Scenario: Выбор по доступности
- **WHEN** несколько агентов подходят для задачи
- **THEN** orchestrator выбирает агента со статусом "ready" и наименьшей загрузкой

#### Scenario: Отсутствие подходящего агента
- **WHEN** ни один агент не может выполнить задачу
- **THEN** orchestrator возвращает ошибку "No suitable agent available for task"

#### Scenario: Учет concurrency limit
- **WHEN** агент уже выполняет максимальное количество задач
- **THEN** orchestrator ставит новую задачу в очередь или выбирает другого агента

### Requirement: Мониторинг выполнения плана
Orchestrator ДОЛЖЕН отслеживать прогресс выполнения плана и отправлять обновления.

#### Scenario: Отправка SSE событий о прогрессе
- **WHEN** задача в плане начинается, выполняется или завершается
- **THEN** orchestrator отправляет SSE события: task_started, task_progress, task_completed

#### Scenario: Обработка успешного завершения задачи
- **WHEN** задача успешно завершена
- **THEN** orchestrator передает результат зависимым задачам и запускает их

#### Scenario: Обработка ошибки в задаче
- **WHEN** задача завершается с ошибкой
- **THEN** orchestrator останавливает зависимые задачи и помечает план как failed

#### Scenario: Частичный успех
- **WHEN** некоторые задачи успешны, но одна failed
- **THEN** orchestrator завершает успешные задачи и возвращает partial_success с деталями

### Requirement: Кеширование планов
Orchestrator ДОЛЖЕН кешировать похожие планы для ускорения планирования.

#### Scenario: Сохранение плана в cache
- **WHEN** план успешно создан и выполнен
- **THEN** orchestrator сохраняет план в Redis с ключом на основе hash запроса

#### Scenario: Использование закешированного плана
- **WHEN** пользователь отправляет похожий запрос
- **THEN** orchestrator находит план в cache и использует его вместо создания нового

#### Scenario: Инвалидация cache
- **WHEN** конфигурация агентов изменяется
- **THEN** orchestrator инвалидирует все планы в cache связанные с этими агентами

#### Scenario: TTL cache
- **WHEN** план сохраняется в cache
- **THEN** TTL устанавливается на 24 часа

### Requirement: Производительность планирования
Orchestrator ДОЛЖЕН создавать планы с минимальной задержкой.

#### Scenario: Планирование простых запросов
- **WHEN** запрос требует 1-2 задачи
- **THEN** планирование завершается менее чем за 2 секунды

#### Scenario: Планирование сложных запросов
- **WHEN** запрос требует 3+ задачи с зависимостями
- **THEN** планирование завершается менее чем за 5 секунд

#### Scenario: Timeout планирования
- **WHEN** планирование занимает более 5 секунд
- **THEN** orchestrator возвращает fallback план с последовательным выполнением

### Requirement: Хранение планов в PostgreSQL
Orchestrator ДОЛЖЕН сохранять полное состояние планов в PostgreSQL для восстановления после перезагрузки сервиса.

#### Scenario: Сохранение плана при создании
- **WHEN** план создан
- **THEN** план сохраняется в БД с таблицей task_plans со следующими полями:
  - id (UUID, primary key)
  - user_id (UUID, foreign key to users)
  - project_id (UUID, foreign key to user_projects)
  - session_id (UUID, foreign key to chat_sessions)
  - original_request (string)
  - status (enum: created, pending_approval, executing, completed, failed, partial_success)
  - total_estimated_cost (float)
  - total_estimated_duration (float)
  - requires_approval (boolean)
  - approval_reason (string, nullable)
  - created_at, started_at, completed_at (datetime with timezone)

#### Scenario: Сохранение задач плана
- **WHEN** план создан
- **THEN** каждая задача сохраняется в таблице task_plan_tasks со следующими полями:
  - id (UUID, primary key)
  - plan_id (UUID, foreign key to task_plans)
  - task_id (string: logical ID like task_0, task_1, ...)
  - description (string)
  - agent_id (UUID, foreign key to user_agents)
  - dependencies (JSON: list of task_ids)
  - estimated_cost (float)
  - estimated_duration (float)
  - risk_level (enum: LOW, MEDIUM, HIGH)
  - status (enum: pending, executing, completed, failed, aborted)
  - result (JSON, nullable)
  - error (string, nullable)
  - created_at, started_at, completed_at (datetime with timezone)

#### Scenario: Восстановление после перезагрузки
- **WHEN** сервис перезагружается во время выполнения плана
- **THEN** orchestrator может восстановить полное состояние плана из БД и продолжить выполнение

#### Scenario: Индексы для оптимизации
- **WHEN** запрашиваются планы
- **THEN** используются индексы по (user_id, session_id), (plan_id), (status, created_at) для быстрого поиска

#### Scenario: Промежуточные результаты
- **WHEN** задача завершена
- **THEN** результат сохраняется в БД (task_plan_tasks.result) для использования зависимыми задачами

#### Scenario: Полное состояние выполнения
- **WHEN** план выполняется
- **THEN** все этапы (pending, executing, partial_success, completed, failed) сохраняются в БД для аудита

#### Scenario: TTL в Redis для кеша
- **WHEN** план сохранен в БД
- **THEN** план также кешируется в Redis (TTL 24 часа) для быстрого доступа к похожим запросам
