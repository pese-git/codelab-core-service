# 📢 Анонс: Уточнение архитектуры доступа к Workspace

**Дата:** 19 февраля 2026  
**Автор:** Команда архитектуры  
**Статус:** ℹ️ Информационное уведомление (изменений в коде нет)

---

## 🎯 Главные моменты

### 1. Что изменилось?

**Архитектура остается той же, но теперь она явно документирована.** Основное уточнение:

- **Workspace** (файловая система пользователя) находится на стороне пользователя (CLIENT)
- **Backend ресурсы** (agent_cache, Agent Bus, Qdrant) управляются на backend
- **Агенты** получают доступ к workspace ТОЛЬКО через tools

```
User/Client FS
        ↓
Agent (backend)
        ↓
Tool (backend)
        ↓
Request → CLIENT (валидирует, выполняет)
        ↓
Result ← Backend
```

### 2. Что НЕ изменилось?

- ❌ **Никаких изменений в коде**
- ❌ **Никаких изменений в API**
- ❌ **Никаких изменений в поведении системы**

Существующая реализация уже следует этой архитектуре!

### 3. Почему это важно?

✅ **Для разработчиков:**
- Теперь ясно где находится workspace
- Ясно как агенты получают доступ к файлам
- Ясно как обеспечивается изоляция между пользователями

✅ **Для архитекторов:**
- Явно задокументированы граничные условия
- Понимание how system design works
- Reference для будущих изменений

✅ **Для операций:**
- Изоляция пользователей явно задокументирована
- Понимание как управляются ресурсы
- Reference для troubleshooting

---

## 📚 Что почитать

### Основная документация
1. **Спецификация User Worker Space** - [`openspec/changes/implement-core-service/specs/user-worker-space/spec.md`](../../openspec/changes/implement-core-service/specs/user-worker-space/spec.md)
   - Раздел "Workspace Access Architecture" - новое
   - Tool Signatures с примерами

2. **REST API документация** - [`doc/rest-api.md`](./rest-api.md)
   - Раздел "Архитектура Workspace и Backend ресурсов" - новое

3. **Changelog** - [`doc/CHANGELOG_WORKSPACE_ARCHITECTURE.md`](./CHANGELOG_WORKSPACE_ARCHITECTURE.md)
   - Полный список всех изменений

### Для разработчиков tools
1. Прочитайте раздел "Tool Signatures" в спецификации
2. Посмотрите примеры валидации доступа
3. Убедитесь что ваш tool следует процессу (request → CLIENT → execute → result)

### Для разработчиков agents
- Никаких действий не требуется
- Используйте tools как раньше, они автоматически обеспечивают доступ

---

## 🔒 Безопасность

### Как обеспечивается изоляция?

**На уровне middleware:**
- JWT токен извлекается из Authorization header
- user_id инжектируется в request.state
- Все запросы фильтруются по user_id

**На уровне Worker Space:**
- Каждый проект имеет отдельный Worker Space (per-project)
- Qdrant collections изолированы: `user{id}_project{id}_{agent}_context`
- Redis ключи используют user_prefix: `user{id}:...`

**На уровне tools (будущие implementations):**
- Tools получают user_id из JWT (через request.state)
- CLIENT валидирует что путь находится в workspace границах
- CLIENT проверяет что user_id совпадает

### Что НЕ может сделать агент?

❌ Получить доступ к файлам другого пользователя  
❌ Выполнить path traversal атаку  
❌ Прямой доступ к файловой системе backend  
❌ Создать/удалить основные директории workspace  

---

## 🚀 Что дальше?

### Следующий шаг: Реализация tools

Документация готова, теперь нужно создать tools:
- `tool_read_file(path, user_id)` - Чтение файлов
- `tool_write_file(path, content, user_id)` - Запись файлов
- `tool_list_directory(path, user_id)` - Листинг директорий

**Требования для tools:**
1. Получить path и user_id как параметры
2. Отправить запрос на CLIENT для валидации
3. CLIENT валидирует путь (в пределах workspace)
4. CLIENT выполняет операцию с локальной FS
5. CLIENT возвращает результат

**Примеры signatures задокументированы в спецификации.**

---

## ❓ Часто задаваемые вопросы

### Q: Нужно ли менять мой код?
**A:** Нет, это чисто документационное изменение. Существующий код уже правильный.

### Q: Мой код соответствует архитектуре?
**A:** Скорее всего да. Код-ревью подтвердил что все компоненты соответствуют архитектуре. Если вы разрабатываете новый tool, проверьте что он следует документированному процессу.

### Q: Где найти примеры?
**A:** 
- Tool signatures: спецификация User Worker Space
- Validation примеры: спецификация User Worker Space
- Code review отчет: `doc/WORKSPACE_ARCHITECTURE_REVIEW.md`

### Q: Это повлияет на production?
**A:** Нет. Это только документация. Система работает как раньше.

### Q: Когда нужно прочитать эту документацию?
**A:**
- ✅ Обязательно если разрабатываете новый tool
- ✅ Обязательно если работаете с изоляцией пользователей
- ✅ Полезно для общего понимания system design
- ❌ Не требуется для использования существующих agents

---

## 📋 Чек-лист для разработчиков

### Если вы разрабатываете новый tool:
- [ ] Прочитал раздел "Tool Signatures" в спецификации
- [ ] Мой tool принимает `path` и `user_id` как параметры
- [ ] Мой tool отправляет запрос на CLIENT
- [ ] CLIENT валидирует путь перед операцией
- [ ] Мой tool возвращает результат на backend

### Если вы модифицируете User Worker Space:
- [ ] Проверил что изменение соответствует архитектуре
- [ ] Изменение изолирует ресурсы per-project
- [ ] Логирую все операции
- [ ] Прочитал код-ревью отчет

### Если вы работаете с agents:
- [ ] Агент использует tools для работы с файлами (не direct FS)
- [ ] Не делаю никакой валидации пути (делает tool/CLIENT)
- [ ] Не пытаюсь обойти tools для доступа к файлам

---

## 📞 Контакты

Вопросы или замечания по документации?
- Ревью документации: В процессе
- Feedback: Приветствуется в PR к docs

---

## 📝 История изменений

| Версия | Дата | Описание |
|--------|------|----------|
| 0.2.0 | 2026-02-19 | Уточнение архитектуры доступа к workspace |

---

**Это информационное уведомление. Активных действий не требуется.**

Спасибо за внимание! 🙏
