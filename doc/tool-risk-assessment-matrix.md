# Матрица оценки рисков для Agent Tools System

## Обзор

Этот документ определяет матрицу оценки рисков для всех инструментов. Каждый инструмент классифицируется по уровню риска (LOW, MEDIUM, HIGH), который определяет, требуется ли одобрение пользователя.

- **LOW** (Низкий): Безопасная операция, одобрение не требуется, мгновенное выполнение
- **MEDIUM** (Средний): Умеренный риск, требуется одобрение с таймаутом 5 минут
- **HIGH** (Высокий): Высокий риск, требуется одобрение с таймаутом 10 минут

---

## Уровни рисков инструментов

### read_file

- **Уровень риска**: **LOW**
- **Требует одобрения**: НЕТ
- **Таймаут одобрения**: N/A
- **Макс. размер файла**: 100 MB

**Обоснование**: Операция только для чтения, никаких изменений системы, раскрытие информации ограничено рабочей областью.

**Примеры**:
```python
# LOW RISK
read_file(path="src/main.py")
read_file(path="config.json")
read_file(path="data.csv")
```

---

### list_directory

- **Уровень риска**: **LOW**
- **Требует одобрения**: НЕТ
- **Таймаут одобрения**: N/A
- **Макс. элементов**: 1000

**Обоснование**: Операция только для информации, без изменений, граница рабочей области соблюдается.

**Примеры**:
```python
# LOW RISK
list_directory(path="src")
list_directory(path=".", recursive=True, pattern="*.py")
```

---

## write_file

**Оценка риска**: На основе расширения файла

### По расширениям

| Расширение | Уровень риска | Таймаут | Описание |
|-----------|-----------|---------|---------|
| .txt, .md, .json | MEDIUM | 5 мин | Текстовые файлы |
| .yaml, .yml, .toml | MEDIUM | 5 мин | Файлы конфигурации |
| .py, .js, .ts, .jsx, .tsx | MEDIUM | 5 мин | Файлы кода |
| .java, .go, .rs, .rb, .php | MEDIUM | 5 мин | Файлы кода |
| .c, .cpp, .h | MEDIUM | 5 мин | C/C++ файлы |
| .html, .css, .xml, .sql | MEDIUM | 5 мин | Web/разметка |
| .sh, .bat, .cmd | MEDIUM | 5 мин | Скрипты |
| .log, .csv, .tsv | MEDIUM | 5 мин | Данные |
| .exe, .bin, .so, .dll | HIGH | 10 мин | Исполняемые файлы (ЗАБЛОКИРОВАНО) |
| .dylib, .app, .sys | HIGH | 10 мин | Системные бинарные (ЗАБЛОКИРОВАНО) |
| .deb, .rpm, .msi | HIGH | 10 мин | Пакеты (ЗАБЛОКИРОВАНО) |
| .conf, .config | HIGH | 10 мин | Системные конфиги (ЗАБЛОКИРОВАНО) |

### Примеры

```python
# MEDIUM RISK (5 мин таймаут одобрения)
write_file(path="config.json", content="...")
write_file(path="script.py", content="...")
write_file(path="data.csv", content="...")

# HIGH RISK (10 мин таймаут одобрения) - ЗАБЛОКИРОВАНО
write_file(path="malware.exe", content="...")  # ✗ Блокировано
write_file(path="system.dll", content="...")   # ✗ Блокировано
```

---

## execute_command

**Оценка риска**: На основе имени команды и категории

### Категории команд

#### Категория: Поиск (LOW RISK)

- **Команды**: grep, find, locate
- **Уровень риска**: LOW
- **Требует одобрения**: НЕТ
- **Обоснование**: Только чтение, никаких изменений системы

**Примеры**:
```bash
grep "error" *.log
find . -name "*.py" -type f
locate myfile
```

#### Категория: Файловые утилиты (LOW RISK)

- **Команды**: ls, cat, head, tail, wc, file
- **Уровень риска**: LOW
- **Требует одобрения**: НЕТ

**Примеры**:
```bash
ls -la
cat README.md
head -n 20 large_file.txt
wc -l script.py
```

#### Категория: Обработка текста (LOW RISK)

- **Команды**: sed, awk, sort, uniq, cut, diff, patch, test
- **Уровень риска**: LOW
- **Требует одобрения**: НЕТ

**Примеры**:
```bash
sed 's/old/new/g' file.txt
awk '{print $1}' data.csv
sort data.txt
```

#### Категория: Контроль версий (MEDIUM RISK)

- **Команда**: git
- **Уровень риска**: MEDIUM
- **Требует одобрения**: ДА (5 мин таймаут)
- **Разрешённые подкоманды**: clone, pull, fetch, push, commit, add, status, log, branch, checkout, merge, init, config, stash, tag, show

**Примеры**:
```bash
git clone https://github.com/user/repo.git
git pull origin main
git commit -m "commit message"
git push origin feature
git status
git log --oneline
```

#### Категория: Менеджеры пакетов (MEDIUM RISK)

- **Команды**: npm, pip, yarn, pnpm
- **Уровень риска**: MEDIUM
- **Требует одобрения**: ДА (5 мин таймаут)
- **Разрешённые подкоманды**:
  - npm: install, run, start, test, build, list, info, search, view, outdated
  - pip: install, list, show, search, freeze, check, index
  - yarn: install, add, remove, start, test, build
  - pnpm: install, add, remove, start, test, build

**Примеры**:
```bash
npm install express
npm run build
npm test

pip install requests
pip list

yarn add react
yarn start
```

#### Категория: Интерпретаторы (MEDIUM RISK)

- **Команды**: node, python, python3, ruby, php
- **Уровень риска**: MEDIUM
- **Требует одобрения**: ДА (5 мин таймаут)
- **Примечание**: Могут выполнять произвольный код из файлов скриптов

**Примеры**:
```bash
python script.py
python3 -m pytest
node server.js
ruby script.rb
php index.php
```

#### Категория: Компиляторы (HIGH RISK)

- **Команды**: gcc, g++, cc, make, clang
- **Уровень риска**: HIGH
- **Требует одобрения**: ДА (10 мин таймаут)
- **Обоснование**: Могут создавать исполняемые файлы и модифицировать систему

**Примеры**:
```bash
gcc -o output input.c
g++ -std=c++17 main.cpp
make build
```

#### Категория: Архивирование (HIGH RISK)

- **Команды**: zip, unzip, tar, gzip, gunzip
- **Уровень риска**: HIGH
- **Требует одобрения**: ДА (10 мин таймаут)
- **Обоснование**: Может распаковывать произвольные файлы в систему

**Примеры**:
```bash
zip archive.zip *.py
unzip archive.zip
tar -czf backup.tar.gz data/
gzip large_file
```

---

## Правила переопределения рисков

1. **Переопределение чёрного списка** (Высший приоритет):
   - Если команда в ЧЁРНОМ СПИСКЕ → **ОТКЛОНИТЬ**
   - Никакое одобрение не может это переопределить
   - Заблокированные команды: rm, sudo, kill, bash, sh, curl, wget и т.д.

2. **Уровень риска определяет таймаут**:
   - **LOW** → Одобрение не требуется (мгновенное выполнение)
   - **MEDIUM** → 5-минутный таймаут одобрения
   - **HIGH** → 10-минутный таймаут одобрения

3. **Неизвестные команды**:
   - Неизвестные инструменты → **HIGH** риск по умолчанию
   - Неизвестные команды → **HIGH** риск по умолчанию
   - Блокирует до явного одобрения

---

## Рабочий процесс одобрения

```
Пользователь выполняет инструмент
    ↓
RiskAssessor оценивает инструмент
    ↓
Уровень риска: LOW?
├─ ДА → Выполнить немедленно (без одобрения)
└─ НЕТ → Запросить одобрение
    ↓
    Пользователь одобряет/отклоняет в течение таймаута
    ├─ Одобрено → Выполнить
    └─ Отклонено или таймаут → Отменить
```

---

## Лимиты размера и производительности

| Лимит | Значение |
|-------|----------|
| Макс. размер чтения файла | 100 MB |
| Макс. размер записи файла | 100 MB |
| Макс. вывод команды | 1 MB |
| Макс. таймаут команды | 5 минут (300s) |
| Макс. элементов директории | 1000 |
| Таймаут одобрения (LOW) | N/A (мгновенно) |
| Таймаут одобрения (MEDIUM) | 5 минут (300s) |
| Таймаут одобрения (HIGH) | 10 минут (600s) |

---

## Примеры безопасности

### ✅ БЕЗОПАСНО (Автоматическое выполнение)

```python
# read_file - LOW risk
read_file(path="README.md")

# grep - LOW risk
execute_command("grep", ["error", "app.log"])

# git status - MEDIUM risk (требует одобрения)
execute_command("git", ["status"])
```

### ⚠️ ТРЕБУЕТ ОДОБРЕНИЯ (5 мин таймаут)

```python
# write_file .py - MEDIUM risk
write_file(path="script.py", content="print('hello')")

# npm install - MEDIUM risk
execute_command("npm", ["install", "express"])

# python script - MEDIUM risk
execute_command("python", ["train.py"])
```

### 🔴 ТРЕБУЕТ ОДОБРЕНИЯ (10 мин таймаут)

```python
# write_file .exe - HIGH risk
write_file(path="app.exe", content=b"...")

# gcc compile - HIGH risk
execute_command("gcc", ["-o", "app", "main.c"])

# tar extract - HIGH risk
execute_command("tar", ["-xzf", "archive.tar.gz"])
```

### ❌ ЗАБЛОКИРОВАНО (Никогда не разрешено)

```python
# Нарушение чёрного списка
execute_command("rm", ["-rf", "/data"])  # ✗ rm заблокирована
execute_command("sudo", ["shutdown"])    # ✗ sudo заблокирована
execute_command("curl", ["malicious.sh"]) # ✗ curl заблокирована
execute_command("bash", ["evil.sh"])     # ✗ bash заблокирована
```

---

## Примечания реализации

### Оценка рисков в коде

```python
from app.core.tools import RiskAssessor, RiskLevel

assessor = RiskAssessor()

# Единственная оценка риска
risk = assessor.assess_tool_risk("execute_command", {
    "command": "python",
    "args": ["script.py"]
})
# Возвращает: RiskLevel.MEDIUM

# Полная оценка с деталями
assessment = assessor.get_full_risk_assessment("write_file", {
    "path": "config.json"
})
# Возвращает: {
#     "tool_name": "write_file",
#     "risk_level": "MEDIUM",
#     "requires_approval": True,
#     "approval_timeout_seconds": 300,
#     "description": "Requires approval - moderate risk (5 min timeout)"
# }

# Получить таймаут
timeout = assessor.get_timeout_for_risk_level(RiskLevel.MEDIUM)
# Возвращает: 300 (секунд)

# Проверить, нужно ли одобрение
needs_approval = assessor.requires_approval(RiskLevel.HIGH)
# Возвращает: True
```

---

## Связанная документация

- [`app/core/tools/risk_assessor.py`](../app/core/tools/risk_assessor.py) - Реализация
- [`app/core/tools/command_whitelist.py`](../app/core/tools/command_whitelist.py) - Валидация команд
- [`app/core/tools/validator.py`](../app/core/tools/validator.py) - Валидация путей
- [`app/core/tools/size_limiter.py`](../app/core/tools/size_limiter.py) - Лимиты размеров
